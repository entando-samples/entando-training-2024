import { Config } from "../models/config.model";

export const mfeConfig: Config = {
    "systemParams": {
        "api": {
            "hero-api": {
                "url": "http://localhost:8081"
            }
        }
    }
}