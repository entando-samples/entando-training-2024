import { Config } from "../models/config.model";

export const config: Config = {
    "systemParams": {
        "api": {
            "hero-api": {
                "url": "http://localhost:8081"
            }
        }
    }
}